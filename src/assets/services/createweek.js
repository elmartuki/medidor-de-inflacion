export function createWeek(event, week, variacion) {
  // event.preventDefault();
  // const weeklist = obtenerDelLocalStorage("Semanas") || [];
  // const nuevaSemana = { week, variacion };
  // const nuevoListado = [...weeklist, nuevaSemana];
  // guardarEnLocalStorage("Semanas", nuevoListado);
}
